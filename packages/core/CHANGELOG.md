# @trysquare/core

## 0.2.0

### Minor Changes

- `when(fn, { description })` — optional component description flows through to the test object and doc generator
- Input objects now accept optional `description` and `type` fields for API documentation annotations
- Test objects gain `componentDescription` field

## 0.1.0

Initial release.
